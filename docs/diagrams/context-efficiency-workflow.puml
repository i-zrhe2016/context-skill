@startuml
!theme plain

title 上下文节省工作循环

start
:提取目标、范围、约束和验收标准;
:检查项目入口和适用指令;
:用 rg 定位文件、符号和调用点;
:读取最小相关片段;

if (输出或上下文过大?) then (是)
  :截断输出并筛选失败/警告;
  :更新五项短状态;
endif

:实施最小修改;
:执行与改动匹配的局部验证;

if (共享接口或架构变化?) then (是)
  :扩大到模块、集成或完整验证;
else (否)
  :保持验证范围最小;
endif

if (仍有未解决问题?) then (是)
  :保留风险和证据位置;
  :从 next step 继续;
else (否)
  :报告实际修改、检查和剩余问题;
endif

stop
@enduml
